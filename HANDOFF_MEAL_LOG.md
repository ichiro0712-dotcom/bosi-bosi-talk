# 要件書: もち経由の食事記録 (チャットアプリ ⇄ Hub Platform)

作成: 2026-07-28 / 作成者: Claude (hub-platform セッション)
宛先: **チャットアプリ (bosi-bosi-talk) のセッション**
状態: **Hub 側 (H-1/H-2/H-3) 完了**。 もち側で T3〜T5 の再テスト待ち (2026-07-29)

---

## 0. これは何か (30 秒で分かる要約)

もちのチャットで「唐揚げ食べた」 と言うと、 **もちが HA (Hub Agent) を呼び出して
Google スプレッドシートに食事を記録し、 HA が返した集計・アドバイスをもちが代弁する**。

```
[ユーザー] 「食事記録」 ツール ON →「唐揚げとサラダ食べた」
   ↓
[もち] hub_chat("唐揚げとサラダ食べた。 記録して", user_hint) を呼ぶ   ← もち側で作る
   ↓
[Hub] 栄養素を推計 → スプシに追記 → 今日の合計 / 7日傾向 / 次の食事アドバイス を生成
   ↓  (非同期。 /api/external-notify に push で返る)
[もち] 「記録したもち！ 今日はここまでで 850kcal だもち〜」 と代弁
```

**もち ⇄ Hub の通信路は既に本番稼働している。 新規に作るのは「食事記録ツール」 の皮だけ。**

---

## 1. 前提: 既にあるもの / これから作るもの

### 1.1 ✅ 既にある (作らなくてよい)

| もの | 実体 | 場所 |
|---|---|---|
| もち → Hub の MCP 呼び出し | `hub_chat` / `hub_cancel` (JSON-RPC 2.0) | `app/api/mochi/route.ts:1019-1132` |
| Hub → もち の完了通知 | `POST /api/external-notify` → messages 挿入 + Web Push | `app/api/external-notify/route.ts` |
| 認証トークン | `HUB_MCP_TOKEN` / `MOCHI_EXTERNAL_NOTIFY_TOKEN` | `.env.local` に投入済 |
| 話者の識別 | `user_hint: 'milk' | 'merry'` を Hub に送る仕組み | `userIdToHint()` `route.ts:1023-1027` |
| Hub 側の食事記録機能 | `hub_log_meal` / `hub_read_meal_summary` | hub-platform `5c02ab1` |

### 1.2 🔨 これから作る

| # | 何を | どこ | 担当セッション |
|---|---|---|---|
| ~~**H-1**~~ | ~~`hub_chat` がツールを使えるように~~ → ✅ **完了 (2026-07-29)** | hub-platform | 完了 |
| ~~**H-2**~~ | ~~ぼし用シートの用意 + シート出し分け~~ → ✅ **完了 (2026-07-28)** | hub-platform | 完了 |
| ~~**H-3**~~ | ~~ぼし (merry) に食事記録権限を付与~~ → ✅ **完了 (2026-07-29)** | hub-platform (DB) | 完了 |
| **M-1** | 「食事記録」 ツール (UI トグル + ツール定義 + 委譲) | チャットアプリ | **チャットアプリ側** |
| **M-2** | 非同期完了 (task_id) の見せ方を食事記録向けに調整 | チャットアプリ | **チャットアプリ側** |
| **M-3** | SoT 同期 (CLAUDE.md が要求する 5 点セット) | チャットアプリ | **チャットアプリ側** |

> ✅ **Hub 側は全て完了・本番反映済**。 もち側はコード変更なしで T3〜T5 を再テストできる。

---

## 2. (解決済) かつて動かなかった理由 — H-1 の背景記録

`hub_chat` は Hub 側で `orchestrator-bridge` → **multi-agent 経路**を実行する。
この経路は **LLM にツールを 1 つも渡していない** (`generateContent` を tools なしで呼ぶ)。

- 実測: `lib/(agent)/mcp/orchestrator-bridge.ts` は `tools` を 0 回しか参照しない
- つまり今もちから「食事記録して」 と投げても `hub_log_meal` は**呼ばれない**
- これは 2026-07-28 に修正した「食事記録して が無言で終わる」 バグと**同じ経路**

→ **対応済 (2026-07-29)**: routing 直後・capability ガードより前でツールが要る依頼を
横取りし、 `runChatWithTools` (Sonnet 固定 + 初回ツール強制) で完遂させる形にした。
ツール不要な依頼は従来どおり multi-agent に流すので既存挙動は変わらない。

---

## 3. 契約 (チャットアプリ側が依存してよい仕様)

### 3.1 送信: `hub_chat`

既存の `callHubChat()` をそのまま使う。 **引数の形は変えない**。

```ts
callHubMcp('hub_chat', {
  message: '<自然言語の依頼>',        // 例: 「唐揚げ 1人前とサラダを食べた。 食事記録して」
  user_hint: 'milk' | 'merry',        // 必須相当。 これでシートが切り替わる
  callback_origin: 'mochi',           // 既存どおり
})
```

**message の作り方 (重要)**
- ユーザーの発話をほぼそのまま渡してよい。 栄養素の推計は **Hub 側の LLM がやる**。
  もち側で kcal を計算しようとしないこと。
- 「記録して」 の意図が伝わる文にすること (例: 末尾に「食事記録して」 を付ける)。
- 時刻の言及 (「お昼に」「20時に」) があれば**message にそのまま残す**。
  Hub 側が本文の時刻を優先して記録する仕様になっている。

**🔴 `user_hint` は必ず送ること (最重要)**
記録先スプレッドシートは **Ichiro 用 / ぼし用の 2 枚**に分かれており、
Hub 側は `user_hint` を見て書き込み先を決める。

| もち側 `user_hint` | Hub 側の記録先 |
|---|---|
| `milk` | Ichiro のシート (`体重・食事記録`) |
| `merry` | **ぼしのシート** (`体重・食事記録 _ぼし`) |
| 送らない / 未知の値 | **記録せず「どちらの記録?」 と聞き返す** |

- 既存の `callHubChat` は `userIdToHint()` で既に送っているので**追加実装は不要**。
  ただし**食事記録では必須**になった点に注意 (欠けると記録されず聞き返しになる)。
- Hub は**推測しない**。 特定できない場合は必ず聞き返す仕様
  (取り違えは追記なので気付きにくく、 後から直しにくいため安全側に倒している)。
- どうしても本文で上書きしたい場合は、 message に「ぼしの分」 等と明示すれば
  Hub 側の LLM がそちらを優先する。

### 3.2 受信: 常に非同期で返る

`callback_origin: 'mochi'` を渡すと、 Hub は **必ず即座に `task_id` を返して非同期実行**する
(`external-server.ts:280-291` で確認済。 同期待ちはしない)。

```jsonc
// hub_chat の即時レスポンス
{ "task_id": "...", "status": "running", "estimated_seconds": 180 }
```

完了後、 Hub が `POST /api/external-notify` に以下を送る (既存実装が処理済):

```jsonc
{
  "type": "task_completed",
  "task_id": "...",
  "origin": "mochi",
  "origin_user_hint": "milk",
  "text": "<HA が生成した記録結果 + 集計 + アドバイス の本文>",
  "metadata": { }
}
```

`external-notify` は **LLM を通さずテンプレ引用**で `messages` に入れる仕様
(`formatHubReply`: `お返事きたもち！\n---\n{text}`)。 これは意図的
(コスト 0 / 数値改変リスク 0)。 → §4.2 で扱いを決める。

### 3.3 Hub が返してくる text の中身

`hub_log_meal` → `hub_read_meal_summary` が走った後、 以下 4 点がこの順で入る:

1. 今回記録した内容 (品目 + この食事の小計 kcal/P/F/糖質/繊維)
2. 今日の累計と目標比 (残り何 kcal か。 超過なら超過と明示)
3. 過去 7 日の傾向 (平均 + 目立つ傾向、 体重推移があれば併記)
4. 次の食事のアドバイス (食品名と目安量まで具体的に)

全体 400〜700 字程度。 **数値が多いので、 もちが言い換えると壊れる** (→ §4.2)。

---

## 4. チャットアプリ側の実装要件

### 4.1 M-1: 「食事記録」 ツール

**A. UI**
- もちモードのトグルと同じ場所 (`+` アタッチメニュー) に **「食事記録」** を追加する。
- 選択中は入力欄のプレースホルダを「何を食べた?」 等に変え、 状態バーに表示する
  (もちモードの `isMochiMode` と同じ作り方でよい)。
- **スマホで押せること**。 `opacity-0 group-hover:*` のような hover 依存で出す UI にしない
  (タッチ端末では永久に押せなくなる。 hub-platform で実際に事故った)。

**B. ツール定義 (`mochiTools`)**
新規に `log_meal` を足すのではなく、 **既存の `hub_chat` に委譲するのが最小構成**。
ただし「食事記録ツールが ON のときは必ず hub_chat に投げる」 を保証したいので、
以下のどちらかを選ぶ:

- **推奨: プロンプト + 明示フラグ方式**
  `POST /api/mochi` の body に `tool: 'meal_log'` を足し、 route 側で
  「このリクエストは食事記録。 必ず `hub_chat` を呼ぶこと」 をシステムプロンプトに注入する。
  → 新ツール追加が不要で、 CLAUDE.md の 5 点セット同期も最小で済む。
- 代替: `log_meal` という専用 FunctionDeclaration を足し、 ハンドラ内で `callHubChat` を呼ぶ。
  → 明示的だが、 CLAUDE.md の同期対象が増える。

**C. 呼び出しが確実に起きること (最重要)**
もち側の LLM は `gemini-3.5-flash-lite`。 **短文 (「唐揚げ食べた」) だと function calling が
不発になり、 ただの雑談応答で終わるリスクがある** (hub-platform 側で同種の事故が実際に発生し、
Sonnet 強制 + ツール強制で回避した)。 対策を必ず入れること:

- 食事記録ツール ON のときは、 **LLM の判断に任せず route 側で決め打ちで `callHubChat` を呼ぶ**
  のが最も確実 (LLM に選ばせない)。
- LLM に選ばせる設計にするなら、 最低限 **呼ばれなかった場合のフォールバック**を用意し、
  「記録できなかった」 ことをユーザーに**必ず伝える** (黙って雑談で終わらせない)。

**D. 1 周ループ制約**
もち側のツールループは 1 周のみ (tool 結果を見た後の再呼び出し不可、 `route.ts:958-975`)。
→ 食事記録は Hub 側 1 タスクで完結するので**問題にならない**。 ループ改修は不要。

### 4.2 M-2: 非同期完了の見せ方

食事記録は「記録された」 という**確信**が要る操作なので、 沈黙が最も困る。

1. **投げた直後**: もちが即座に「記録するもち！ ちょっと待ってね」 と返す
   (既存の task_id ハンドリングと同じ。 task_id は絶対にユーザーに見せない)。
2. **完了時**: `/api/external-notify` 経由でチャットに出る。
   - **現在の実装はテンプレ引用** (`お返事きたもち！---{text}`)。
     食事記録は数値が多いので、 **引用のままを推奨** (言い換えると数値が壊れる)。
   - もし「もちが代弁する」 演出を強めたいなら、 **数値部分は原文のまま残し、
     前後にもちの一言を足すだけ**にすること。 過去に Gemini 言い換えを入れて
     撤回した経緯がある (`487114d` → `cd6a80b`)。 同じ轍を踏まない。
3. **失敗時**: Hub が `ok:false` を返したら、 **成功したふりをしない**。
   「記録できなかったもち…」 と理由付きで伝える。

### 4.3 M-3: SoT 同期 (CLAUDE.md 準拠)

チャットアプリの `CLAUDE.md` は、 もちの機能追加時に**同時更新**を要求している。
実装と同じコミットで以下を揃えること:

- [ ] `APP_SPEC.md` (= もちのシステムプロンプトに**丸ごと注入される**ので必須)
- [ ] `app/mochi-settings/page.tsx` の `CAPABILITIES` (「できること」 タブ)
- [ ] DB の `couple_settings.mochi_prompt` (冪等な `scripts/update-mochi-prompt-*.mjs` を作る)
- [ ] `mochiTools` 配列 (専用ツール方式を採る場合)
- [ ] ディスパッチハンドラ (同上)

> 📌 ついでに直すとよい既知のズレ (任意):
> - `APP_SPEC.md` はモデルを `gemini-2.5-flash` と書いているが、 実コードは `gemini-3.5-flash-lite`。
>   **APP_SPEC はプロンプトに注入されるので、 間違いがそのままもちに教え込まれている**。
> - `.env.local.example` が古く、 `GEMINI_API_KEY` / `HUB_MCP_*` / VAPID を 1 つも書いていない
>   (使われていない Firebase 系だけが載っている)。

---

## 5. hub-platform 側でやること (このセッションの担当・参考情報)

チャットアプリ側は**待たなくてよい**が、 何が保証されるかを知っておくこと。

### H-1: `hub_chat` をツール実行経路に載せる → ✅ **完了 (2026-07-29)**

**実装済** (commit `c0889d1` + `cadd66e`、 本番デプロイ済)。 もち側は**コード変更不要**。

Hub 側で実機 e2e 済 (もちと同じ `hub_chat` + `callback_origin:'mochi'` で呼び出し):
- ✅ ぼしのシートに行が追加され、 SUMIFS の日次合計も正しく計算された
  (3 品目 → 214kcal / P32.7 / F7.1 / 糖質1.8 / 繊維3.7)
- ✅ `[CHOICES]` / `[icon:]` の生タグは**出なくなった** (この経路では禁止する指示を明示)
- ✅ 確認ステップを踏まず 1 往復で完了する
- ✅ ぼし (merry) の capability も付与済 (H-3 完了)

途中で見つけて直したバグ 2 件:
1. **書式なしシートで集計が常に 0** … Sheets API 既定 (FORMATTED_VALUE) は
   セル書式に従うため、 書式のある Ichiro シートは `"2026-07-18"`、 書式が無い
   新規シート (ぼし) は `"46232"` (シリアル値の**文字列**) を返す。 後者を
   日付として解釈できず、 ぼし側だけ合計が 0 になっていた (エラーは出ない)。
2. **ぼしが capability で拒否される** … ツール経路を agent capability ガードより
   後ろに置いていたため、 routing が選んだ agent の capability がぼしの許可リストに
   無く弾かれていた。 ガードより前に移動して解消。

→ **もち側で T3〜T5 を再テストしてください。** 通る想定です。

<details><summary>旧記述 (実装前)</summary>
</details>

### (参考) H-1 の設計
`lib/(agent)/mcp/orchestrator-bridge.ts` を `runChatWithTools` 経由に変更し、
`hub_log_meal` / `hub_read_meal_summary` を含むツールを露出する。
→ これが済むと、 **食事記録に限らず HA の全ツール** (Drive 読み書き / カレンダー /
プロジェクト照会) がもち経由で使えるようになる。

### H-2: メリー用シートの用意と出し分け 🔴
**現状の Hub 実装は「Drive から `体重・食事記録` という名前のスプシを 1 つ探す」 だけで、
ユーザーによる出し分けをしていない。 このままだとメリーの記録が Ichiro のシートに入る。**

#### 実データ (2026-07-28 実測)

| | 値 |
|---|---|
| Ichiro (`owner=ichiro` / もち側 `milk`) | `1JCs-45VMrI_-i7-Gz1S95CiKpX21gfcezrNgUA_FNGA` (`体重・食事記録`) |
| **ぼし** (`owner=boshi` / もち側 `merry`) | `1eDqKrI5shZ9UpSInTRJ0JyxhdpzEIW-sKkdscIvZbfk` (`体重・食事記録 _ぼし`) |
| 置き場所フォルダ | `1uDNvvqKxv34Y9kfmlOkmHYnz2A8J6TH0` (`食事記録`) |

#### H-2 ステータス: ✅ **完了 (2026-07-28)**

- ぼし用シートは Ichiro が作成済 (bot は storageQuota=0 で新規作成不可のため手作業。
  bot への共有も設定済で読み書き可能を実測確認)
- Hub 側の出し分けを実装済 (詳細は下記)

#### 実装済みの内容

**1. owner による出し分け**
- `hub_log_meal` / `hub_read_meal_summary` に **`owner` パラメータ** (`ichiro` | `boshi`) を追加
- もち経由の場合は **`user_hint` (milk / merry) からも解決**できる
- シート特定は **ID 直指定**に変更 (env `MEAL_SHEET_ID_ICHIRO` / `MEAL_SHEET_ID_BOSHI` で上書き可)。
  名前検索は廃止 — 同じフォルダに `体重・食事記録` と `体重・食事記録 _ぼし` が並ぶため、
  前方一致だと**他人のシートを掴む**危険があった

**2. 🔴 曖昧なら聞き返す (Ichiro 指示)**
owner が特定できない場合、 **既定値に倒さず** `reason: "needs_owner"` を返す。
LLM はこれを受けて「Ichiro さんとぼしさん、 どちらの記録ですか?」 と聞き返す。
取り違えは追記なので**気付きにくく後から直しにくい**ため、 安全側に倒している。
判定表 (実測):

| owner | user_hint | 結果 |
|---|---|---|
| `ichiro` / `ぼし` / `メリー` / `BOSHI` 等 | — | 解決する |
| — | `milk` / `merry` | 解決する |
| なし | なし | **needs_owner (聞き返す)** |
| 未知の値 | 未知の値 | **needs_owner (聞き返す)** |

**3. 体重記録の当日行を自動生成 (`ensureWeightRow`)**
体重記録タブの実績列は SUMIFS 数式で、 **行が存在して初めて集計が出る**。
ぼしのシートはヘッダのみだったため、 これが無いと日次合計が永久に空になる。
→ 記録時に当日行が無ければ SUMIFS つきで自動生成する。
実測: ぼしシートに 2 品 (148kcal / P24 / 糖質5 / 繊維3) を書いて
SUMIFS が正しく計算されることを確認済 (テスト行は削除済)。

#### ⚠️ ぼしシートの残作業 (Ichiro)

**目標タブが空**。 このままだと目標比較ができず、 HA は
「目標が設定されていないので比較できません」 と正直に返す (数字は捏造しない)。
ぼし用の目標を入れる場合は、 目標タブに 1 行足す:
`フェーズ / 目標_kcal / 目標_P(g) / 目標_F(g) / 目標_糖質(g) / 目標_繊維(g) / 開始日`

### H-3: メリーの権限付与
`mcp_authorization` の実データ (2026-07-28 時点):

| origin | user_hint | allowed_capabilities |
|---|---|---|
| mochi | milk | `["*"]` (全許可) |
| mochi | merry | `["reservation","travel_search","general_qa"]` |

→ **メリーは現状 `*` ではないため、 食事記録の capability を明示的に足す必要がある。**
(milk は `*` なのでそのまま通る)

---

## 6. 完了条件 (受け入れテスト)

チャットアプリ側の実装が終わったら、 以下を**実機**で確認する。

- [ ] **T1**: ミルクが「食事記録」 ON で「唐揚げとサラダ食べた」 → もちが「記録するもち」 と即返す
- [ ] **T2**: 数十秒後、 記録結果 + 今日の合計 + 7日傾向 + 次の食事アドバイス がチャットに出る
- [ ] **T3**: Ichiro のスプシ「体重・食事記録」 の 食事記録タブに**行が増えている**
- [ ] **T4**: 体重記録タブの当日の 実績_kcal 等 (SUMIFS) が**自動で増えている**
- [ ] **T5**: メリーが同じ操作 → **メリーのシートに入る** (Ichiro のシートに入らないこと)
- [ ] **T6**: 「食事記録」 ツールがスマホで**押せる**
- [ ] **T7**: わざと Hub を止める等で失敗させたとき、 **黙って終わらず**「記録できなかった」 と言う
- [ ] **T8**: 数値 (kcal 等) が言い換えで改変されていない

---

## 7. 設計判断の記録 (なぜこの形か)

**Q. 「HA に話しかけるボタン」 を別に作る案ではないのか?**
A. 却下。 理由:
1. 同じチャットに「もちの人格」 と「HA の人格」 が混在し、 ユーザーが毎回どちらに
   話すか選ばされる。 もちが窓口で裏で委譲する方が体験が一貫する。
2. もち ⇄ Hub の通信路が既にあるので、 委譲方式は**追加コストがほぼゼロ**。
3. どちらの案でも **H-1 (hub_chat のツール対応) は必要**。 別ボタン案はそれに加えて
   新しい UI と表示経路が要るだけで、 得るものがない。

**Q. 栄養素の推計はどちら側でやるのか?**
A. **Hub 側**。 もちは自然言語をそのまま渡すだけ。
理由: 推計ロジックとスプシの列構造を 1 箇所 (Hub) に閉じ込めるため。
もち側にも推計を持たせると、 2 箇所が同じルールを持つことになり必ずズレる。

**Q. なぜ非同期なのか (同期で返せないのか)?**
A. Hub 側が `callback_origin` 指定時は必ず非同期にする実装。
もち側も `maxDuration = 60` の制約があるため、 同期待ちは筋が悪い。

---

## 8. 参考: Hub 側スプシの構造 (デバッグ時に見る用)

チャットアプリ側は直接触らないが、 T3/T4 の確認で見ることになる。

- スプシ名: **体重・食事記録** (Ichiro 用)
- **食事記録** タブ … 1 行 = 1 **品目** (1 食ではない)。 14 列:
  `日付 / 時刻 / 品目 / 摂取割合(%) / カロリー(kcal) / タンパク質(g) / 脂質(g) /
   炭水化物(g) / 食物繊維(g) / ビタミンB1(mg) / βカロテン(µg) / カリウム(mg) / 鉄分(mg) / 備考`
- **体重記録** タブ … 1 行 = 1 日。 実績列は食事記録を参照する **SUMIFS 数式**
  → 食事記録に追記すれば日次合計は自動更新される (手で書かない)
- **目標** タブ … フェーズ制 (開始日つき)。 アドバイスはその日のフェーズと比較する
- 糖質 = 炭水化物 − 食物繊維 (食事記録に糖質列は無い)
